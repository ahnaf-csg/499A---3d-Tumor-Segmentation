# DO NOT PIN numpy, torch, scipy or scikit-image on Colab.
# Reinstalling them triggers source builds and can leave numpy's compiled
# binaries mismatched with its Python files ("numpy.dtype size changed").
# Colab ships all four. Install only the pure-Python additions below.
#
#   pip install monai --no-deps
#   pip install einops nibabel
#
# The MONAI version is NOT pinned: models.build_model tries both SwinUNETR
# signatures, so it works whether img_size is required (<=1.2), deprecated
# (1.3-1.4) or removed (>=1.5). Validated on 1.6.0.

monai>=1.3
einops>=0.7
nibabel>=5.0

# Already present in Colab -- listed for non-Colab installs only:
# torch>=2.3          (torch.amp.GradScaler signature)
# numpy>=1.24
# scipy>=1.10         (connected components, Wilcoxon)
# scikit-image>=0.21  (connected-components fallback)
# matplotlib>=3.7     (XAI figures)
# pandas>=2.0
